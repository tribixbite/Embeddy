*.iml
.gradle
/local.properties
/.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
*.jks
*.keystore
!debug.keystore
output/
*.hprof
tools/aapt2-arm64/
/tools/
!site/src/**/tools/

# Node / Astro
node_modules/
.astro/
dist/
site/dist/

# Kotlin incremental compilation metadata
.kotlin/

# Literal "~" dirs created by the bun-on-termux wrapper's HOME handling
**/~/

# Locally built FFmpeg AAR (F-Droid produces this on its buildserver).
# Must never be committed: F-Droid requires in-repo binaries be deleted and
# rebuilt, and it is ~80 MB.
/app/libs/*.aar
