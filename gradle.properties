# JVM args — constrained for Termux ARM64
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true
android.nonTransitiveRClass=true

# Disable configuration cache (stability on Termux)
org.gradle.configuration-cache=false

# Parallel builds
org.gradle.parallel=true

# Kotlin
kotlin.code.style=official

# AAPT2: use local qemu wrapper for ARM64 Termux builds (x86_64 aapt2 via qemu-x86_64)
android.aapt2FromMavenOverride=/data/data/com.termux/files/home/git/Embeddy/tools/aapt2/aapt2
